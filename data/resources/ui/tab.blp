using Gtk 4.0;
using Adw 1;

template $Tab: Adw.Bin {
  Gtk.Stack stack {
    Gtk.ScrolledWindow scroll_win {
      vexpand: true;

      Adw.ClampScrollable clamp {
        halign: center;
        maximum-size: 768;
        tightening-threshold: 720;
      }
    }
  }
}